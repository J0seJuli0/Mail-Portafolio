{
  "version": 2,
  "builds": [
    { "src": "api/send_email.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/send_email.py" }
  ],
  "env": {
    "SMTP_USER": "@smtp_user",
    "SMTP_PASS": "@smtp_pass"
  }
}